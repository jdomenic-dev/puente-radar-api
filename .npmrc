# Required for NestJS with pnpm.
# NestJS relies on reflect-metadata and decorator resolution which requires
# hoisted node_modules. Without this, TypeORM and NestJS decorators may fail
# to resolve at runtime.
shamefully-hoist=true
strict-peer-dependencies=false
