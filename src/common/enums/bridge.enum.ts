export enum BridgeStatus {
  Low = 'low',
  Medium = 'medium',
  High = 'high',
  Saturated = 'saturated',
}

export enum WaitTrend {
  Rising = 'rising',
  Steady = 'steady',
  Falling = 'falling',
}
