services:
  postgres:
    image: postgres:16-alpine
    container_name: puente-radar-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: puente_radar
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
